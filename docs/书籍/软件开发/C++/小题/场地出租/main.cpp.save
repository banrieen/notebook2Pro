#include <iostream>

using namespace std;

class padding
{
    private :
    char a;
    int b;
    doub c;
    public:
    padin(char c=a;int i=100;doub d=2.3):a(c),b(i),c(d) {}
}
int main()
{
    cout << "Hello world!" << endl;
    padding a;
    std::cout <<"size of padding == "<<sizeof(padding)<<endl ;
    char * p=(char *)(&a);
    int int_len=sizeof(int);
    std::cout<<*p<<endl;
    std::cout<<int_len<<endl;
        
    return 0;
}
